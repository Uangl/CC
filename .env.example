# 复制为 .env 并填入后端地址，然后重启：npx expo start -c
#
# 不填也能运行：未配置时使用本地模拟模式。

# ── 后端地址 ─────────────────────────────────────────────
# 启动后端后填入地址。手机端请用电脑的局域网 IP（如 192.168.1.100）
# 例：EXPO_PUBLIC_API_URL=http://192.168.1.100:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
